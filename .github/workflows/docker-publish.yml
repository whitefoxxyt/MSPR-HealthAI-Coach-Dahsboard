name: Docker - Publish to GHCR

on:
  workflow_call:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: whitefoxxyt/mspr-front

jobs:
  build-and-push:
    name: Build et push de l'image sur GHCR
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Récupération du code
        uses: actions/checkout@v4

      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Configuration Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extraction des métadonnées (tags et labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix=,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build et push de l'image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VITE_AUTH_BASE_URL=${{ vars.VITE_AUTH_BASE_URL || 'http://localhost:3000' }}
            VITE_API_BASE_URL=${{ vars.VITE_API_BASE_URL || 'http://localhost:8080/api' }}
            VITE_AI_NUTRITION_BASE_URL=${{ vars.VITE_AI_NUTRITION_BASE_URL || 'http://localhost:8001' }}
            VITE_RECO_FITNESS_BASE_URL=${{ vars.VITE_RECO_FITNESS_BASE_URL || 'http://localhost:8002' }}
            VITE_ADMIN_EMAILS=${{ vars.VITE_ADMIN_EMAILS || 'admin@healthai.local' }}
            VITE_USE_MOCK=false
          cache-from: type=gha
          cache-to: type=gha,mode=max
